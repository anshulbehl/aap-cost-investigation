---
- name: EC2 Inventory and Cost Report
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    aws_region: "{{ region | default(config.aws.region | default('us-east-1')) }}"
    include_stopped: "{{ include_stopped_instances | default(true) }}"

  tasks:
    - name: Load configuration defaults
      ansible.builtin.include_vars:
        file: "{{ playbook_dir }}/../config/defaults.yml"
        name: config

    - name: Verify configuration loaded
      ansible.builtin.assert:
        that:
          - config is defined
          - config.aws is defined
          - config.aws.region is defined
        fail_msg: >
          Configuration file not found. Ensure the project root contains config/defaults.yml.

    - name: Set effective tag aliases
      ansible.builtin.set_fact:
        effective_tag_aliases: "{{ tag_aliases | default(config.tags.aliases | default({})) }}"

    - name: Gather all EC2 instances
      amazon.aws.ec2_instance_info:
        region: "{{ aws_region }}"
        filters: "{{ omit if include_stopped else {'instance-state-name': 'running'} }}"
      register: ec2_instances

    - name: Build instance summary
      ansible.builtin.set_fact:
        instance_summary: >-
          {{
            ec2_instances.instances | map('combine', {})
            | map('dict2items')
            | list
          }}
        running_count: "{{ ec2_instances.instances | selectattr('state.name', 'equalto', 'running') | list | length }}"
        stopped_count: "{{ ec2_instances.instances | selectattr('state.name', 'equalto', 'stopped') | list | length }}"
        total_count: "{{ ec2_instances.instances | length }}"

    - name: Display instance overview
      ansible.builtin.debug:
        msg:
          total_instances: "{{ total_count }}"
          running: "{{ running_count }}"
          stopped: "{{ stopped_count }}"
          region: "{{ aws_region }}"

    - name: Display instance details
      ansible.builtin.debug:
        msg:
          instance_id: "{{ item.instance_id }}"
          instance_type: "{{ item.instance_type }}"
          state: "{{ item.state.name }}"
          launch_time: "{{ item.launch_time }}"
          private_ip: "{{ item.private_ip_address | default('N/A') }}"
          public_ip: "{{ item.public_ip_address | default('N/A') }}"
          name: "{{ item.tags.Name | default('UNTAGGED') }}"
          environment: "{{ item.tags.Environment | default(item.tags[effective_tag_aliases['Environment'] | default('Environment')] | default('UNTAGGED')) }}"
          owner: "{{ item.tags.Owner | default(item.tags[effective_tag_aliases['Owner'] | default('Owner')] | default('UNTAGGED')) }}"
          cost_center: "{{ item.tags.CostCenter | default(item.tags[effective_tag_aliases['CostCenter'] | default('CostCenter')] | default('UNTAGGED')) }}"
          platform: "{{ item.platform_details | default('Linux/UNIX') }}"
          vpc_id: "{{ item.vpc_id | default('N/A') }}"
          subnet_id: "{{ item.subnet_id | default('N/A') }}"
      loop: "{{ ec2_instances.instances }}"
      loop_control:
        label: "{{ item.instance_id }} ({{ item.instance_type }})"

    - name: Group instances by type
      ansible.builtin.set_fact:
        instance_type_counts: >-
          {% set counts = {} %}
          {% for inst in ec2_instances.instances %}
          {%   set _ = counts.update({inst.instance_type: counts.get(inst.instance_type, 0) + 1}) %}
          {% endfor %}
          {{ counts }}

    - name: Display instance type counts
      ansible.builtin.debug:
        msg: "{{ instance_type_counts }}"

    - name: Set stats for workflow artifact passing
      ansible.builtin.set_stats:
        data:
          ec2_total_instances: "{{ total_count }}"
          ec2_running_instances: "{{ running_count }}"
          ec2_stopped_instances: "{{ stopped_count }}"
          ec2_instance_types: "{{ instance_type_counts }}"
          ec2_region: "{{ aws_region }}"
